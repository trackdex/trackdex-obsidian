# 0.1-11 — Регистрация команд (shell, stable IDs)

**Milestone:** 0.1 — Foundation and project skeleton  
**Тип:** UI / Obsidian integration

## Цель

Зарегистрировать все v1-команды со **стабильными ID** из TECHNICAL_DESIGN §12.2; callbacks — no-op или «coming soon» через Notice, без прототипных ID.

## Зависимости

- **0.1-07** — [DI-контейнер и bootstrap](./0.1-07-di-container-bootstrap.md)
- **0.1-10** — [Основа i18n](./0.1-10-i18n-foundation.md) (локализованные `name`)

## Объём работ

- `infrastructure/obsidian/commands-registry.ts` (или `ui/commands/` + вызов из composition):
  - `scan-or-resume-indexing`
  - `reindex-places`
  - `make-current-note-place`
  - `edit-current-place-geometry`
  - `pause-indexing`
  - `reset-rebuild-index`
- Удалить регистрацию из `main.ts` прототипных команд (`open-trackdex-overview`, `insert-trackdex-tag`, `scan-tracks-folder`) **или** перенести в **0.1-13** — в этой задаче новые ID должны быть единственными indexing-related.
- `name`: через `t('commands....')`; `id` — только ASCII kebab-case, без переименований.
- `checkCallback` где нужен контекст (active markdown note) — возвращать `false` если контекст недоступен.

## Вне scope

- Реальная логика scan/reindex/reset (**0.3**, **0.7**, **0.9**).

## Условия завершения

- [ ] Все 6 command ID из TECHNICAL_DESIGN §12.2 зарегистрированы и видны в Command palette.
- [ ] Ни один v1 command ID не совпадает с удаляемыми prototype ID из **0.1-13** (кроме явно задокументированной миграции).
- [ ] Вызов команды показывает локализованный Notice «not implemented yet» или no-op без crash.
- [ ] `npm run build` проходит.

## Артефакты

- `src/infrastructure/obsidian/commands-registry.ts` (или согласованный путь)
- Вызов `registerCommands(container)` из composition/bootstrap

## Связанные задачи

- **0.1-13** — окончательное удаление legacy commands из `main.ts`
