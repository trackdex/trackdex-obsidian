# 0.1-13 — Удаление / миграция legacy прототипа

**Milestone:** 0.1 — Foundation and project skeleton  
**Тип:** рефакторинг / breaking cleanup

## Цель

Убрать или мигрировать настройки и команды прототипа, конфликтующие с v1 scope: `tracksFolder`, custom basemap tile URL в settings, prototype scan command и прочие demo-элементы.

## Зависимости

- **0.1-07** — [DI-контейнер и bootstrap](./0.1-07-di-container-bootstrap.md)
- **0.1-11** — [Регистрация команд](./0.1-11-commands-registration-shell.md) (чтобы palette не содержал дубликатов)

Опционально после **0.1-12**, если track view ещё читает `basemapTileUrl` — тогда временно оставить read-only migration.

## Объём работ

- **Settings:**
  - Удалить `tracksFolder` из `TrackdexSettings` и UI; v1 — vault-wide scan (**0.3**), не папка по умолчанию.
  - Убрать `basemapTileUrl` / `basemapStyleUrl` из persisted settings **или** перенести в будущий раздел map provider (**0.9** / backlog); в 0.1 — удалить из settings tab, hardcode/default в map layer только если view ещё нужен для demo GPX.
  - `migrateSettings`: удалить legacy keys из сохранённого JSON (one-time migration).
- **Commands:** удалить `scan-tracks-folder`, `open-trackdex-overview`, `insert-trackdex-tag` (или заменить на v1 equivalents только если ID совпадают с планом — иначе удалить).
- **UI:** убрать `TrackdexModal` demo, status bar «ready» при желании заменить на нейтральный stub.
- Обновить `README.md` при упоминании tracks folder / basemap settings.

## Вне scope

- Полная settings tab v1 (**0.9**).
- Реализация vault-wide scanner (**0.3**).

## Условия завершения

- [ ] В `loadData()` после миграции в сохранённых settings нет ключей `tracksFolder`, `basemapTileUrl`, `basemapStyleUrl` (или они игнорируются без UI).
- [ ] Command palette не содержит prototype command IDs из списка IMPLEMENTATION_PLAN 0.1 scope.
- [ ] Grep по репозиторию: нет user-facing строк «Scan tracks folder» / привязки scan к `tracksFolder`.
- [ ] `npm run build` и smoke test проходят.
- [ ] Существующие пользователи прототипа: при загрузке плагин не падает на старом JSON settings.

## Артефакты

- Обновлённые `src/settings.ts`, `src/main.ts`, `README.md`
