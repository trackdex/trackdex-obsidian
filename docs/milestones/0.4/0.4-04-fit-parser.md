# 0.4-04 — FIT parser (fit-file-parser)

**Milestone:** 0.4 — Parser normalization and computed metrics  
**Тип:** infrastructure / parsers

## Цель

`.fit` via `fit-file-parser` per §2.5; runtime dependency in bundle.

## Зависимости

- **0.4-01**
- **0.1-06**

## Объём работ

- `fit-parser.ts`.
- Move `fit-file-parser` to dependencies.

## Вне scope

- Garmin SDK path.

## Условия завершения

- [x] `.fit` fixture → ParsedTrack.
- [x] Bundle measure documented.

## Артефакты

- `fit-parser.ts`
