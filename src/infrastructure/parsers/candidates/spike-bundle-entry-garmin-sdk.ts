/**
 * Entry for scripts/measure-bundle.mjs — @garmin-fit/sdk candidate weight.
 * Not loaded by the Obsidian plugin at runtime.
 */
import { Decoder, Stream } from "@garmin-fit/sdk";

export function touchGarminSdkBundle(): void {
	void Decoder;
	void Stream;
}

touchGarminSdkBundle();
